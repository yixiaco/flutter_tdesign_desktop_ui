import 'package:flutter/widgets.dart';

/// 导航菜单-顶部导航
/// 用于承载网站的架构，并提供跳转的菜单列表。
class THeadMenu extends StatefulWidget {
  const THeadMenu({Key? key}) : super(key: key);

  @override
  State<THeadMenu> createState() => _THeadMenuState();
}

class _THeadMenuState extends State<THeadMenu> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
